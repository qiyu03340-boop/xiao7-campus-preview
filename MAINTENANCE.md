# 小7 v2.7.9 修改位置说明

本版在原有反馈状态透明化、校园服务与新生指南数据分离基础上，增加微信内置浏览器单指滚动兼容修复。

## 1. 反馈功能在哪里修改

文件：`index.html`

搜索：`feedbackNotice`

当前规则：
- 点击“有帮助”“没解决”“信息有误”时，不再显示“已记录”或“已收到”。
- 页面会明确提示：反馈后台尚未接入，本次操作不会上传或保存。
- “联系学姐”仍然进入人工帮助页面。

以后接入真实后台后，只需要在这一段把按钮点击事件替换成真实提交接口；接口成功返回后，才可以显示“已提交”。

## 2. 新生指南内容在哪里修改

文件：`data/guide-content.js`

这里只维护：
- 报到
- 宿舍
- 食堂与快递
- 学业
- 专业目录
- 浴室热水
- 安全提醒
- 校园生活
- 奶茶店
- 校园通知
- 助学贷款
- 请假外出

其中原来的“食堂与快递”正文完整保留。以后修改校园服务，不会再碰到这里。

## 3. 校园服务在哪里修改

文件：`data/campus-services.js`

这里只维护4个校园服务：
1. 校园快递代拿
2. 健身房情况了解
3. 26届广东岭南新生校园群
4. 广东岭南职业技术学院店铺推荐

字段说明：
- `title`：校园服务页标题
- `homeTitle`：首页短标题
- `subtitle`：校园服务页副标题
- `icon`：图标类型
- `action: "profile"`：点击后进入学姐页面
- `action: "detail"`：点击后打开详情弹窗
- `items`：详情正文

首页和校园服务页都会自动读取这份数据，不需要修改两遍HTML。

## 4. 不要再修改的旧文件

`data/services.js` 已删除。以后不要重新创建或使用它，否则会再次把新生指南和校园服务混在一起。

## 5. 微信单指下滑修复在哪里

文件：`index.html`

搜索：`微信内置浏览器兼容`

当前规则：
- 页面、按钮、链接和输入框交回浏览器进行原生触摸手势判断。
- 不再全局强制 `touch-action: manipulation`，避免微信 WebView 把卡片上的滑动误判为点击。
- AI 聊天区、清单、详情弹窗和 FAQ 列表仍明确使用 `touch-action: pan-y`。
- 不要在 `body`、`.app-shell`、`.page` 或大面积按钮上新增 `touch-action: none`。
